<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Job tracker</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-LN+7fdVzj6u52u30Kp6M/trliBMCMKTyK833zpbD+pXdCLuTusPj697FH4R/5mcr" crossorigin="anonymous">
</head>
<div class="alert alert-success alert-dismissible fade show" role="alert">
  <%= success %>
</div>
<body>
    <h2>Your all applications</h2>
    <br>
    <a href="/application/new" class="add-btn">➕ Add New Application</a>
    <br>
    <br>
<table class="table">
  <thead>
    <tr>
      <th scope="col">#</th>
      <th scope="col">Job Title</th>
      <th scope="col">Company</th>
      <th scope="col">Date</th>
      <th scope="col">Status</th>
      <th scope="col">Notes</th>
      <th scope="col">Edit/Delete application</th>
    </tr>
  </thead>
  <tbody>
    <% apps.forEach((app, index) => { %>
      <tr>
        <th scope="row"><%= index + 1 %></th>
        <td><%= app.jobTitle %></td>
        <td><%= app.company %></td>
        <td><%= app.date.toISOString().slice(0,10) %></td>
        <td><%= app.status %></td>
        <td><%= app.notes %></td>
        <td><form action="/application/edit/<%= app._id %>">
            <button class="btn btn-dark">Edit</button>
        </form>
        </td>
        <td>
            <form action="/application/delete/<%= app._id %>?_method=DELETE" method="POST">
            <button class="btn btn-dark">Delete</button>
        </form>
        </td>
      </tr>
    <% }) %>
  </tbody>
</table>
</body>
</html>